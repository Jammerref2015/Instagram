function App() {
  return (
    <div className="App">
      <p>Hello</p>
    </div>
  );
}

export default App;


// client side rendered app: react (Cra)
// -> database (firebase)
// -> react-loading-skeleton
// tailwind

// folder structure

// src
  // -> components, 
  // -> constants, 
  // -> context, 
  // -> lib (firebase)
  // -> helpers (firebase)
  // -> services 
  // -> styles
  // -> hooks